name: UiPath-CI
env: 
  LOCAL_SCRIPT_DIR: C:\Users\riedelna\OneDrive - Sykes Enterprises, Incorporated\Desktop\CIScripts
on: [push]
jobs:
  Build:
    runs-on: self-hosted
    steps:
      - name: Clone repo
        uses: actions/checkout@v3
      - name: Build
        run: py "$env:LOCAL_SCRIPT_DIR\Build.py" -i ${{ github.workspace }} -o $env:LOCAL_SCRIPT_DIR\temp\package
  DevDeploy:
    runs-on: self-hosted
    needs: Build
    steps:
      - name: Deploying
        run: echo "Deployed to Dev"
  TestDeploy:
    runs-on: self-hosted
    needs: Build
    steps:
      - name: Deploying
        run: echo "Deployed to Test"
